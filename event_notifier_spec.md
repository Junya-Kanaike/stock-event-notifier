# 日本株イベント Slack通知システム 仕様書

- 版: v2.0
- 更新日: 2026-09-08
- 対象: PO、IPO、立会外分売、CB、株式分割
- 実行基盤: Python 3.12 / GitHub Actions / Slack Incoming Webhook
- 日付・時刻: JST固定。営業日は土日、日本の祝日、年末年始の東証休業日を除外する。

## 1. 共通の状態遷移

| 判定結果 | 通知動作 |
|---|---|
| 対象確定 | 各イベントの個別仕様に従い予定を登録 |
| 情報不足 | 売買文言と予定は付与せず、`#system` の日次集約で確認 |
| 判定待ち → 対象確定 | 各イベントの個別仕様に従い予定を開始 |
| 判定待ち → 対象外 | 追加通知なし。未解決一覧から除外 |
| 最初から対象外 | 通知なし |
| 重複検知 | 開示ID、イベントID、予定ラベルで抑止 |
| 日程変更 | 変更通知を送信。送信済みラベルは維持し、未送信予定を新日程で再作成 |
| 中止・撤回 | 未送信予定を全停止。チャンネル通知の有無は各イベントの個別仕様に従う |

寄り付き向け通知が09:00以降、大引け向け通知が15:30以降になった場合は、売買指示を削除して「参考通知」にする。8日以上経過した未送信予定は自動送信せず、未解決として `#system` に集約する。

## 2. PO — `#po`

対象は「貸借または信用」「PRO Marketではない」「吸収規模が一度でも80億円以上」の全てを満たす案件。

### 規模判定

- 価格決定前: `想定吸収規模 = PO発表日の終値 ×（公募株数＋売出株数＋OA株数）`
- 15:30前は前営業日終値で暫定判定し、15:30以降に発表日のYahoo Finance raw closeへ更新する。
- 価格決定後: `確定吸収規模 = 公募株数×発行価格 ＋ 売出株数×売出価格 ＋ OA株数×売出価格`
- `po_threshold_ever_met` に80億円到達をラッチし、その後の値下がりで通知対象から外さない。
- 株価、必要株数、信用区分などが不明な間は `#po` に通知しない。
- 対象確定時の `[PO予定通知]` と1つ下の6予定通知だけを `#po` へ送信する。価格決定資料、払込完了、判定待ち、吸収規模更新だけでは通知しない。
- 中止・撤回はstateの予定を停止するだけで、`#po` には通知しない。

### 予定通知

| タイミング | 09:00前 | 09:00以降 |
|---|---|---|
| 価格決定日 | 寄り付きで買う | 参考通知 |
| +1営業日 | 寄り付きで買う | 参考通知 |
| +2営業日 | 寄り付きで買う | 参考通知 |
| 受渡日 | 寄り付きで買う | 参考通知 |
| +25営業日 | 寄り付きで半分売る | 参考通知 |
| +26営業日 | 寄り付きで半分売る | 参考通知 |

+25と+26は同一文言を使い、保有数量の基準は定義しない。価格決定日が期間なら初日を暫定日とし、受渡日が期間なら初日を予定日とする。後日の確定資料はstateと未送信予定の更新にのみ使う。

## 3. IPO — `#ipo`

全IPOが対象で、PRO Marketのみ除外する。売買文言は付けない。

| タイミング | 通知 |
|---|---|
| 上場日の前営業日 | 「明日、銘柄コード・銘柄名が新規上場します」 |
| 上場日 | 「本日、銘柄コード・銘柄名が新規上場します」 |
| 上場日変更 | `[上場日変更]` |
| 上場日取得不能 | `[IPO判定待ち]` |

## 4. 立会外分売 — `#bunbai`

対象は「東証Prime / Standard / Growth」かつ「貸借または信用」。名証等の他市場とPRO Marketは除外する。

| タイミング | 09:00前 | 09:00以降 |
|---|---|---|
| 発表検知 | 銘柄、市場、信用区分、実施予定日 | 同じ |
| 実施日の前営業日 | 「明日、立会外分売実施予定です。申し込み忘れずに！」 | 同じ |
| 実施日 | 寄り付きで買う | 参考通知 |
| +5営業日 | 寄り付きで売却する | 参考通知 |

実施日が期間なら初日を暫定日とし、確定日が異なる場合は `[実施日変更]` を送信する。

## 5. CB — `#cb`

貸借銘柄のみを対象とし、PRO Marketは除外する。日次予定は作らない。

| 状況 | 通知 |
|---|---|
| CB発表検知 | `[CB発表]` を即時通知 |
| 発行額取得不能 | 「発行額: 取得失敗」のまま通知 |
| 信用区分取得不能 | `[CB判定待ち]` |
| 通知後に同日自社株買いを検知 | `⚠️ [取消] 自社株買い同時発表を確認` |
| CB検知時点で同日自社株買いを把握済み | CB通知なし |

## 6. 株式分割 — `#split`

対象は「東証PrimeまたはStandard」かつ「貸借」。Growth、信用銘柄、他市場、PRO Marketは除外する。発表時の通常通知と効力発生日通知は行わない。

### 権利付最終日の決定

1. PDFに権利付最終日が明記されていれば最優先する。
2. 明記がなければ、基準日と東証のT+2決済・営業日カレンダーから暫定計算する。
3. トレーダーズ・ウェブの「権利取最終日」（権利付最終日と同義）を参照し、日付と分割比率を確定補完する。
4. JPXの権利落日公開後、その前営業日を権利付最終日として照合する。
5. 不一致なら予定を停止し、`#system` だけに警告する。`#split` の判定待ち・対象確定通知は行わない。

stateの分割detailは `record_date`、`ex_right_date`、`rights_final_date`、`rights_final_calculation_basis`、`rights_final_status`、`rights_final_confirmed`、`rights_date_conflict` を保持する。

### 予定通知

| 基準 | 目標時刻 | 通知文言 | 遅延時 |
|---|---:|---|---|
| 権利付最終日 +0d | 19:00 | 分割の空売り検討 | 翌日以降は参考通知 |
| +1営業日 | 12:00 | 大引で空売り | 15:30以降は参考通知 |
| +5営業日 | 08:00 | 寄り付きで買い戻し | 09:00以降は参考通知 |

GitHub Actionsは寄り付き向けを06:03から08:43まで20分間隔の9回、11:50/12:00/12:10、18:50/19:00/19:10の冗長cronで起動する。日次同期は05:37に前倒しする。予定ごとの `not_before_jst` と `action_cutoff_jst` を満たす最初のジョブのみが送信し、`sent` フラグで再実行を抑止する。

## 7. `#system`

### 即時通知

- TDnetの全面取得・解析失敗
- JPX銘柄マスター、信用区分、権利落情報、トレーダーズ・ウェブの株式分割情報の取得失敗
- Yahoo株価の再試行失敗（同一銘柄・基準日の警告は重複抑止）
- PDF取得・本文解析の重大エラー
- 3営業日連続のTDnet 0件
- state整合性エラー
- 日付矛盾、JPX照合不一致、予定生成不能

通常の対象外銘柄は `#system` にも送信しない。

### 1日1回の集約

平日は最終TDnet巡回後の20:10 JST、非営業日は朝ジョブで次を集約する。

- イベント別未解決件数、判定待ち銘柄
- POの株価・株数・価格決定日未取得
- 株式分割の基準日・権利付最終日未確定
- 8日以上経過した未送信予定
- データソース別の最終成功時刻
- 当日のSlack通知成功・失敗件数

## 8. 状態・運用

- `state/events.json` の `schema_version` は2。旧stateは起動時に自動移行する。
- 分割の旧 `effective-1bd` / `effective_day` 予定は移行時に削除し、権利付最終日基準の3予定のみを生成する。
- 対象外または中止案件はスケジュールを持たず、30日後にarchiveする。
- Slack失敗時は3回再試行。予定通知の失敗時は `sent` にせず、次の冗長ジョブで再試行する。
- GitHub Actionsの予定時刻は保証されない。時刻ゲートと参考通知化は実際のJST開始時刻で判定する。

## 9. 検証基準

- 土日・祝日・月末基準日とT+2計算
- JPX照合一致・不一致と旧分割予定の除去
- 19:00 / 12:00 / 08:00の `not_before_jst` と15:30 / 09:00のカットオフ
- PO +25 / +26の同一文言、分売前営業日の申込み注意
- 日程変更時の送信済み維持、重複実行抑止、送信失敗後の再試行
- 全対象・対象外・予定通知・中止のSlack dry-runマトリクス
